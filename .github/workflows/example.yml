name: Figure out the best label

on:
  workflow_dispatch:
    inputs:
      pr_name:
        description: >
          PR number for core,
          use "repo/PR#" it the app is deployed for other project
          (will notify in the `repo`)
        required: true
        type: string
        hint: "Hey, I'm a hint!"
        placeholder: "placeholder here"

jobs:
  dummy:
    steps:
      - run: echo "Hello, world"
        shell: bash
